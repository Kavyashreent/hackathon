name: Build & Deploy to EKS

on:
  push:
    branches:
      - main  # or master

jobs:
  docker-eks:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repo
        uses: actions/checkout@v3

      # 2️⃣ Configure AWS credentials
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # 3️⃣ Explicitly set Docker to use ECR
      - name: Login to AWS ECR
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: |
          # Replace with your full ECR URI
          ECR_URI=333813598365.dkr.ecr.us-east-1.amazonaws.com/demo-app-repo

          echo "Logging in to AWS ECR..."
          aws ecr get-login-password --region $AWS_REGION \
            | docker login --username AWS --password-stdin 333813598365.dkr.ecr.us-east-1.amazonaws.com/demo-app-repo

      # 4️⃣ Build Docker image
      - name: Build Docker image
        env:
          GITHUB_SHA: ${{ github.sha }}
        run: |
          ECR_URI=333813598365.dkr.ecr.us-east-1.amazonaws.com/demo-app-repo
          IMAGE_TAG=$(echo $GITHUB_SHA | cut -c1-7)
          docker build -t patient-service:$IMAGE_TAG .
          docker tag patient-service:$IMAGE_TAG $ECR_URI:$IMAGE_TAG
          docker tag patient-service:$IMAGE_TAG $ECR_URI:latest

      # 5️⃣ Push Docker image
      - name: Push Docker image
        env:
          GITHUB_SHA: ${{ github.sha }}
        run: |
          ECR_URI=333813598365.dkr.ecr.us-east-1.amazonaws.com/demo-app-repo
          IMAGE_TAG=$(echo $GITHUB_SHA | cut -c1-7)
          docker push 333813598365.dkr.ecr.us-east-1.amazonaws.com/demo-app-repo:$IMAGE_TAG
          docker push 333813598365.dkr.ecr.us-east-1.amazonaws.com/demo-app-repo:latest

      # 6️⃣ Update kubeconfig for EKS
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig --region ${{ secrets.AWS_REGION }} --name ${{ secrets.EKS_CLUSTER_NAME }}

      # 7️⃣ Deploy to EKS
      - name: Deploy to EKS
        run: |
          IMAGE_TAG=$(echo $GITHUB_SHA | cut -c1-7)
          ECR_URI=333813598365.dkr.ecr.us-east-1.amazonaws.com/demo-app-repo
          kubectl set image deployment/patient-service patient-service=$ECR_URI:$IMAGE_TAG --record
          kubectl apply -f service.yaml
